# here instead of setting the weights manually, we need a link with the weights of the current recommender system to update them automatically

weights = {
    "popularity": 1,
    "nlp": 1,
    "random": 1
}